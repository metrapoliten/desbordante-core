#pragma once

#include <enum.h>

namespace algos {
#include "core/model/enum_visibility_begin.h"
BETTER_ENUM(InputFormat, char, singular = 0, tabular);
#include "core/model/enum_visibility_end.h"
}  // namespace algos
